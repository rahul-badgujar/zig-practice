{
	"version": "2.0.0",
	"tasks": [
        {
            "label": "zig-build",
            "type": "shell",
            "command": "zig build --prefix .zig-out --cache-dir .zig-cache",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}